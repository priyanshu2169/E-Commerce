<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Details</title>
    <link rel="stylesheet" href="/CSS/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">

</head>
<body>
    <br><br>
    <br><br>
    <div class="details offset-3"><h1>Listing Details:</h1></div>
    <div class="card offset-3" style="width: 18rem;">
  <img src="<%= listing.image %>" class="card-img-top show-image" alt="listing">
  <hr class="divider">
  <div class="card-body">
    <p class="card-text">
        <b><%= listing.title %></b><br>
        
       &#8377;<%= listing.price.toLocaleString("en-IN") %>/night <br>
      
    </p>
  </div>
</div><br>
<div class="btns mb-5 offset-3">
        <form method="GET" action="/edit/<%=listing._id%>">
            <button class="btn btn-secondary editBtn">Edit</button>
        </form><br>
          <form method="GET" action="/home">
        <button class="btn btn-primary Back">Back to all products</button>
        </form><br>
         <form method="POST" action="/listing/<%=listing._id%>/delete?_method=DELETE">
            <button class="btn btn-dark deleteBtn">Delete</button>
        </form>
        
</div>
</body>
</html>